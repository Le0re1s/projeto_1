git init
git add
git add .
git clone
git commit
git commit -m
git status
git config --global user.email "leo@gmail.com"
git config --global user.name "Meunome"
git remote add origin (link da publicação no Github)
git push --set-upstream origin master

-------------------------------

No cmd:

cd             change directory
mkdir ou md    make directory
del            deletando arquivos e pastas
rd             (remove directory) permite que façamos a exclusão de uma pasta
ren            renomeando arquivos e pastas
cls or clear   Limpando a tela